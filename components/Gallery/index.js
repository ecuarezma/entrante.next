import React from "react";
import Card from "../Card";
import images from "../images";

const Gallery = props => {
  return (
    <div>
      <h1>{props.page}</h1>
    </div>
  );
};

export default Gallery;
